@using TravelTripProject.Models.Class
@model List<Blog>
<div class="box1-left">
    @foreach (var x in Model)
    {
        <div class="bot-img">
           <a href="/Blog/BlogDetay/@x.ID"> <img src="@x.BlogImage" alt="" style="width:250px;height:90px;" /></a>
</div>
        <div class="bot-desc1">
            <p>@x.Aciklama.Substring(0, 90)...</p>
        </div>
        <div class="clear"></div>
    }
</div>